# MATLAB / Simulink build artifacts
*.asv
*.autosave
*.slxc
*.mexw64
*.mexa64
*.mexmaci64
slprj/
sccprj/
codegen/

# Simulink cache
*.slx.r*
*.slx.bak

# MATLAB workspace dumps
*.mat
# ...but keep shared data files checked in
!data/*.mat

# Logs and temp files
*.log
octave-workspace

# OS files
.DS_Store
Thumbs.db

# Symlink to Report repo (separate submodule)
Report

# Obsidian vault config (if someone opens this folder in Obsidian)
.obsidian/

# (plots now go to docs/images/ on teammate machines; teammates should
# avoid staging those — they're Mads's canonical versions)
